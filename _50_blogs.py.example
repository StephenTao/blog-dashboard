# The slug of the dashboard to be added to HORIZON['dashboards']. Required.
DASHBOARD = 'blogs'

# A list of applications to be added to INSTALLED_APPS.
ADD_INSTALLED_APPS = ['blogdashboard']
DEFAULT = True
ADD_EXCEPTIONS = {}
